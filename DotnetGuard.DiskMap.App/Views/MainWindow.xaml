<Window x:Class="DotnetGuard.DiskMap.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DotnetGuard DiskMap" Icon="/Assets/app.ico" Height="640" Width="1040"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextPrimaryBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" Grid.Row="0" Text="&gt; dotnetguard diskmap // disk usage" FontSize="16" FontWeight="Bold"
                   Foreground="{StaticResource AccentBrush}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="PathBox" Grid.Column="0" VerticalContentAlignment="Center"/>
            <Button Grid.Column="2" Content="Browse" Width="90" Height="28" Click="BrowseButton_Click"/>
            <Button x:Name="ScanButton" Grid.Column="4" Content="Scan" Width="90" Height="28" Click="ScanButton_Click"/>
            <Button x:Name="UpButton" Grid.Column="6" Content="Up" Width="90" Height="28" Click="UpButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <TextBlock Text="Location:" Margin="0,0,6,0" Foreground="{StaticResource TextSecondaryBrush}"/>
            <StackPanel x:Name="BreadcrumbPanel" Orientation="Horizontal"/>
        </StackPanel>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="190"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1"
                    Background="{StaticResource SurfaceBrush}" CornerRadius="4">
                <Grid Margin="2">
                    <Canvas x:Name="TreemapCanvas" ClipToBounds="True" SizeChanged="TreemapCanvas_SizeChanged"/>
                    <TextBlock x:Name="EmptyStateText" Text="Pick a folder and hit Scan."
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="1"
                    Background="{StaticResource SurfaceBrush}" CornerRadius="4">
                <StackPanel Margin="10">
                    <TextBlock Text="File types" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>
                    <StackPanel x:Name="LegendPanel"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center"/>
            <ProgressBar x:Name="ScanProgressBar" Grid.Column="1" Height="18" IsIndeterminate="False"
                         Foreground="{StaticResource AccentBrush}" Background="{StaticResource SurfaceBrush}"
                         Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
